let arr = [25, 65, 84, 79, 41, 56, 45, 65, 21, 56];

// to find the element exist or not in the array you can use includes function

console.log(arr.includes(45));

// you also can use indexOf function

console.log(arr.indexOf(65) != -1);
